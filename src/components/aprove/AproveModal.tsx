import React from "react"
import { Box } from "@mui/material"

interface AproveModalProps {
    name: string
}

export const AproveModal: React.FC<AproveModalProps> = ({}) => {
    return <Box sx={{}}></Box>
}
